[tool.poetry]
name = "realt"
version = "0.1.0"
description = ""
authors = ["Katsiaryna Saroka"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
black = "^23.11.0"
flake8 = "^6.1.0"
python-dotenv = "^1.0.0"
celery = "^5.3.5"
redis = "^5.0.1"
pillow = "^10.1.0"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
